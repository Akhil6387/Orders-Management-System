# Frontend-specific environment variables
# Vite exposes only variables prefixed with VITE_ to the browser bundle.

# In Docker Compose the Nginx proxy forwards /api/* to the backend,
# so the frontend calls relative paths like /api/products.
# For local Vite dev server, use the full backend URL.
VITE_API_URL=http://localhost:8000
